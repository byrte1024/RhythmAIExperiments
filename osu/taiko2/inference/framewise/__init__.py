"""Framewise / sliding-window chart predictors.

Scaffold only — populated when a framewise model is ported. Any
implementation here is a `ChartPredictor` subclass with its own
internal mechanics; no shared ABCs with `autoregressive/`.
"""
