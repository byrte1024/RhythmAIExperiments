{
    "_comment": "#016b-mini trainer: same as #016 except `epochs: 2` (probe stops at 1-2 evals via manual interrupt; 2 caps the upper bound). Identical optimizer / batch / schedule.",
    "__class__": "osu.taiko2.domain.training:TrainerConfig",
    "epochs": 2,
    "batch_size": 64,
    "learning_rate": 3e-4,
    "weight_decay": 0.01,
    "grad_clip": 1.0,
    "evals_per_epoch": 4,
    "amp": false,
    "num_workers": 0,
    "seed": 42,
    "metric_to_watch": "loss",
    "metric_lower_is_better": true
}
